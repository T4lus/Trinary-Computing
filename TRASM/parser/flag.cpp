#include <Trinary/tryte.h>
#include <Utils/utils.h>

#include "../parser.h"

std::vector<Tryte> Parser::STC(std::vector<std::string> _opMap, maps_t maps) {
	checkNbArg(_opMap);
	std::vector<Tryte> memTab;
	
	return memTab;
}

std::vector<Tryte> Parser::CLC(std::vector<std::string> _opMap, maps_t maps) {
	checkNbArg(_opMap);
	std::vector<Tryte> memTab;
	
	return memTab;
}

std::vector<Tryte> Parser::STZ(std::vector<std::string> _opMap, maps_t maps) {
	checkNbArg(_opMap);
	std::vector<Tryte> memTab;
	
	return memTab;
}

std::vector<Tryte> Parser::CLZ(std::vector<std::string> _opMap, maps_t maps) {
	checkNbArg(_opMap);
	std::vector<Tryte> memTab;
	
	return memTab;
}